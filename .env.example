# Example environment variables for Portfolio Analytics V3

# Application environment (development, testing, production)
APP_ENV=development

# Data source API keys
# Note: Yahoo Finance via yfinance does not require an API key.  Leave blank or omit.
ALPHAVANTAGE_API_KEY=
EDGAR_API_KEY=

# Cache configuration
# Directory where cached API responses will be stored
CACHE_DIR=./cache
# Default time‑to‑live for cached responses (in seconds)
CACHE_TTL=86400

# Risk and valuation parameters
RISK_FREE_RATE=0.03
MAX_EXPECTED_RETURN=0.50
RIDGE_ALPHA=1.0

# Logging configuration
LOG_LEVEL=INFO
LOG_FORMAT=json

# Server configuration
API_HOST=0.0.0.0
API_PORT=8000
ENABLE_CORS=true
